# Demo scripts

One file per phase exit demo, written by the task that closes the phase (T039, T056, T064, T076, …). Each script is a numbered list of clicks/commands with the expected observation and, where relevant, the SQL/API check that proves it. The mentor follows the script without the student's help.
