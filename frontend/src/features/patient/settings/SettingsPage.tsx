import { useEffect, useState } from "react";
import { useTranslation } from "react-i18next";
import { getMeta, setMeta } from "../../../db/schema";
export function SettingsPage() { const { i18n } = useTranslation(); const [locked, setLocked] = useState(false); const [slow, setSlow] = useState(false); useEffect(() => { void Promise.all([getMeta("languageLocked"), getMeta("slowSpeech")]).then(([a,b]) => { setLocked(a === "1"); setSlow(b === "1"); }); }, []); return <section className="space-y-6"><h1 className="text-3xl font-bold">Settings</h1><label className="block font-bold">Language<select className="mt-2 min-h-touch w-full rounded-card border-2 border-primary bg-surface px-4" disabled={locked} value={i18n.resolvedLanguage?.split("-")[0] ?? "en"} onChange={(e) => void i18n.changeLanguage(e.target.value)}><option value="en">English</option><option value="as">অসমীয়া</option><option value="bn">বাংলা</option></select></label>{locked ? <p>Language is locked to prevent accidental changes.</p> : null}<label className="flex min-h-touch items-center gap-4 font-bold"><input checked={locked} className="h-8 w-8" type="checkbox" onChange={(e) => { setLocked(e.target.checked); void setMeta("languageLocked", e.target.checked ? "1" : "0"); }} /> Lock language</label><label className="flex min-h-touch items-center gap-4 font-bold"><input checked={slow} className="h-8 w-8" type="checkbox" onChange={(e) => { setSlow(e.target.checked); void setMeta("slowSpeech", e.target.checked ? "1" : "0"); }} /> Speak slowly</label><button className="min-h-touch rounded-card border-2 border-primary px-4" type="button">Replay instructions</button></section>; }
